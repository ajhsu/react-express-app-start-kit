import test from 'tape';
import React from 'react';

import AppContainer from '../views/app-container';

test('formatTime', t => {
  t.equal(1, 1, 'should be equal');
  t.equal(2, 2, 'should be equal');
  t.equal(3, 3, 'should be equal');
  t.end();
});
